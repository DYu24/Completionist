<%= bootstrap_form_with(model: list, local: true) do |form| %>
  <%= form.text_field :name, required: true %>
  <%= form.primary %>
  <%= link_to "Cancel", :back, class: "btn btn-secondary" %>
<% end %>
